Use the integration's declared interface. Report its actual URL or route when it has a web UI,
or its command/API result otherwise; do not invent a page or start an unrelated persistent
process. For cookie SSO, give the URL to the user to open in their normal signed-in browser and
do not automate it. For other paths, perform the authorized verification with available tools,
handing off only user-required actions. Record the actual result before claiming success.
