Build and verify the app in the named host (such as Lovable or Replit), using the recipe's actual
builder prompt. Use available authorized tools to execute the acceptance scenarios there; a
local scaffold or printed checklist is not a hosted-app pass. Hand off only actions that require
the user, such as sign-in or unavailable host controls, and record those results separately.
If required access is unavailable, report the precise blocker rather than claiming completion.
For corpus-dependent examples, use an equivalent topic present in the instance while preserving
the expected behavior and safety requirements below:
