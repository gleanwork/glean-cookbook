{
  "id": "multi-step-agent",
  "title": "Multi-step agent with governed tools",
  "description": "Build a Glean agent that plans, retrieves, and acts through a governed custom tool — with a safe fallback when the tool is denied.",
  "sidebarLabel": "Multi-Step Agent",
  "surfaces": ["agents", "tools"],
  "status": "production-pattern",
  "category": "agent",
  "level": "Advanced",
  "levels": {
    "minimal": true,
    "wow": true
  },
  "timeEstimate": "~2 hr",
  "icon": "agent",
  "requiredScopes": ["AGENTS", "TOOLS"],
  "authMethod": ["client-api-oauth-or-token"],
  "buildMethod": "scaffold",
  "languages": ["python"],
  "prerequisites": [
    "Agent builder access in your Glean instance",
    "Permission to register a custom tool (or admin help)",
    "A Glean token with AGENTS scope for API invocation",
    "Your own email, to put on (and take off) the tool server's allow-list between the two runs"
  ],
  "demoQueries": [
    {
      "query": "Summarize our open incidents and file a tracking ticket",
      "expectedBehavior": "With your email on the tool server's allow-list: the agent summarizes and the governed tool call succeeds (200, a real ticket id). With it removed: the tool returns 403 and the agent falls back to a read-only summary rather than failing the run. The governance check must actually fire, not be assumed."
    }
  ],
  "codeAssets": [
    {
      "repoPath": "recipes/multi-step-agent/tool-server",
      "language": "python",
      "description": "Governed custom Tool — files an incident ticket, restricted to one group"
    },
    {
      "repoPath": "recipes/multi-step-agent/invoke-agent",
      "language": "python",
      "description": "Invoke the agent via agents.run, demo both the permitted and denied branches"
    }
  ],
  "steps": [
    {
      "title": "Scaffold the project",
      "command": "npx tiged --mode=git gleanwork/glean-cookbook/recipes/multi-step-agent multi-step-agent"
    },
    {
      "title": "Set the tool server's allow-list",
      "command": "cd multi-step-agent/tool-server && cp .env.example .env",
      "description": "Set AUTHORIZED_EMAILS to your own email for the first run. You'll remove it for the second run to see the denied branch — changing the allow-list is how both governance paths get demonstrated, since the agent always runs as you. The server refuses to start on an empty allow-list, since that denies everyone and makes the permitted branch unobservable."
    },
    {
      "title": "Run the tool server",
      "command": "cd multi-step-agent/tool-server && uv run server.py",
      "description": "Listens on port 8080. Keep this running in its own terminal — the agent calls it over HTTP once registered."
    },
    {
      "title": "Register the tool",
      "description": "Manual, UI-only step — there is no API to create a tool. In Admin > Platform > Tools > Add, register the tool and upload multi-step-agent/tool-server/openapi.yaml as its API spec."
    },
    {
      "title": "Create the agent",
      "description": "Manual, UI-only step. In Agent Builder: paste the recipe's instructions, turn retrieval on, attach the tool you just registered. Copy the agent's ID for the next step."
    },
    {
      "title": "Set credentials",
      "command": "cd multi-step-agent/invoke-agent && cp .env.example .env",
      "description": "Fill in GLEAN_API_TOKEN, GLEAN_INSTANCE, and GLEAN_AGENT_ID (the ID from the previous step)."
    },
    {
      "title": "Run it",
      "command": "cd multi-step-agent/invoke-agent && uv run main.py",
      "description": "Dependencies are declared inline (PEP 723) and locked, so uv installs them into an isolated environment on first run — no requirements.txt, venv, or activate step."
    },
    {
      "title": "Verify",
      "description": "With your email on the allow-list, confirm the ticket actually gets filed. Then restart the tool server without it and confirm the agent produces a read-only fallback summary instead of a hard failure."
    }
  ],
  "scaffoldActions": [],
  "architecture": [
    {
      "label": "Agent",
      "caption": "plan → retrieve → act",
      "category": "agent",
      "emphasized": true
    },
    {
      "label": "Glean retrieval",
      "caption": "grounded in the index",
      "category": "search"
    },
    {
      "label": "Custom tool",
      "caption": "governed write action",
      "category": "workflow"
    }
  ],
  "aiPrompt": "Build the governed multi-step agent from\nhttps://developers.glean.com/cookbook/multi-step-agent\n\n1. Register the recipe's custom tool (an incident-ticket-creation HTTP\n   tool) in the Glean admin console (Admin > Platform > Tools > Add) —\n   this is a manual/admin-console step, there is no API to create a\n   tool. Upload the recipe's openapi.yaml as the tool's API spec.\n2. Create an agent in the Agent Builder (also a UI step, not an API\n   call): instructions from the recipe, retrieval on, the tool\n   attached. Copy the agent's ID.\n3. Invoke via glean.client.agents.run(agent_id=..., messages=[...])\n   — the agent runs as whoever my credential belongs to, so send no\n   impersonation header and don't ask me for an admin token. NOT\n   chat.create's\n   ChatMessage/ChatMessageFragment shape; agents use Message/\n   MessageTextBlock instead. run_stream() returns raw SSE text (a\n   string), not a parsed event iterator — use run() unless you're\n   ready to parse SSE yourself.\n4. Demo both governance branches by changing the tool server's\n   AUTHORIZED_EMAILS allow-list between runs, not by impersonating\n   anyone: once with my email on it (the ticket is filed), once\n   without (the tool returns 403 and the agent's own instructions\n   produce a read-only summary rather than failing the run). Glean\n   forwards my identity to the tool as the Glean-User-Email header,\n   which is where that authorization is enforced.",
  "llmContext": "Run agents with glean.client.agents.run(agent_id, messages) using Message and MessageTextBlock. run_stream() returns raw SSE text. Register custom tools in the admin console from an OpenAPI specification. Runs use the credential's identity; custom tool servers receive the acting user's email in Glean-User-Email and must enforce authorization there.",
  "goDependency": false,
  "featured": true,
  "tags": ["agents", "governance"]
}
