# Acceptance map — PACT-452

Every step in the ticket, where it lives, and the assertion that proves it.
`npm run verify:fixture` runs all 58 checks with no credentials.

| #   | Ticket requirement                                                                                                       | Implementation                                             | Proof                                                                                                                                                                                                                                                                       |
| --- | ------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | PagerDuty webhook filtered by severity/service; ack in channel within seconds                                            | `passesFilter` + `handleWebhook` in `server.ts`            | `Sev3 below the threshold is filtered out`, `unwatched service is filtered out`, `acked before triage`                                                                                                                                                                      |
| 2   | Resolve service to owner team / runbook / repo via a **service registry**, then fan out retrieval                        | `lib/registry.ts`, `fanOutQueries` in `lib/triage.ts`      | `service registry resolved the on-call engineer`, `service registry resolved the owner`, `escalation window read from the catalog (30 min)`                                                                                                                                 |
| 3   | Triage card: probable cause **ranked by evidence**, similar past incident, runbook step, proposed fix, every claim cited | `lib/evidence.ts` + triage card in `public/index.html`     | `a cause is supported by a matching past incident`, `the supported cause cites the precedent, not a runbook`, `no runbook was used to license a claim about cause`                                                                                                          |
| 4   | Approve/edit/reject **restricted to on-call + service owners**; unapproved proposals **expire and escalate**             | `assertActionable`, `arm`, `escalate` in `lib/approval.ts` | `a non-on-call, non-owner actor is refused with 403`, `the refused approval is audited against that actor`, `the service owner may approve even when not on call`, `expiry escalates`, `escalates to the target named in the catalog`, `expiry does NOT execute the action` |
| 5   | Approve as-is or edit before approving                                                                                   | `approve()` with `edited`                                  | `the edit is attributed to the approver`, `the audit notes that the proposal was edited`                                                                                                                                                                                    |
| 6   | Execute only **pre-registered** actions                                                                                  | `ACTIONS` allowlist; refusal at proposal time              | `action registry is closed and advertised`, `an unregistered action is refused, not offered`, `the refusal names the action the agent tried to take`, `no approval card is offered for it`                                                                                  |
| 7   | **Audit log** on every execution; failures post back, never silent                                                       | `record()` in `lib/state.ts`; channel posts                | `audit records request, approval and execution`, `a failed action is reflected in status`, `the failure is posted to the channel`, `the failure is audited`, `the error text reaches the channel, not just the log`                                                         |
| 8   | Post-resolution **postmortem timeline** as a starting point                                                              | `lib/postmortem.ts`                                        | `a postmortem draft is produced`, `the draft includes a timeline section`, `the timeline is built from audited events`                                                                                                                                                      |
| —   | **Dual implementation**: Glean agent vs app-orchestrated                                                                 | `lib/orchestrators/`                                       | `the agent path reaches the same approval gate`, `the agent path is labelled as agent-planned`, `the agent path classifies evidence with the same rules`                                                                                                                    |
| —   | HITL "must be real, not decorative"                                                                                      | see rows 4, 6, 7                                           | 58 checks; the gate has refusal paths for actor, timing, staleness, double-approval, and capability                                                                                                                                                                         |

## Added beyond the ticket

| Rule                                                           | Why                                                                                                                                                                               | Proof                                                                                                                                 |
| -------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| A mutating action requires an evidence-supported cause         | Without it, an alarm with no precedent still produced a "draft fix PR" card, because the deploy runbook mentions rollbacks. Same relevance-is-not-evidence error, one layer down. | `the mutating action was downgraded, not offered`, `the downgrade is visible in the channel`, `the downgrade is audited as a refusal` |
| Approving an already-executed or escalated incident is refused | A gate that can be re-entered is not a gate.                                                                                                                                      | `approving an already-executed incident is refused with 409`, `approving an escalated proposal is refused with 409`                   |
| Evidence classification stays out of the planner               | Asking the planner to grade its own evidence is asking the wrong entity.                                                                                                          | `the agent path classifies evidence with the same rules`                                                                              |

## Deviations

| Ticket says                                             | Built                                                                                 | Why                                                                                                                              |
| ------------------------------------------------------- | ------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| Execute "as the approving user", per-person permissions | App credential; gate is an app-level policy check, reported as `impersonation: false` | Impersonation was removed in `9a1d1ba`. Overstating it is the one genuinely dangerous thing this recipe could teach.             |
| Dual implementation                                     | Two orchestrators in one shell                                                        | Steven's 7/31 sheet: both variants render into one shell. Duplicating a governance-heavy app doubles where the gate could drift. |
| Full dashboard (7/31 sheet)                             | "Little dashboard"                                                                    | Steve 8/3: "a _little_ dashboard thing." MTTA/MTTR rollup, expiring-soon lane, shift handoff and channel sync are Extensions.    |
| Governed actions via `first-custom-tool`                | Three inert pre-registered actions                                                    | PACT-454 doesn't exist. The registry boundary is real; the executors are simulated so the recipe is safe to run live.            |
| Fable mockup → Frank He                                 | Skipped                                                                               | Same as `customer-360`. Confirm it's optional.                                                                                   |
