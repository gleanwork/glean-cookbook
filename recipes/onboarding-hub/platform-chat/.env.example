GLEAN_API_TOKEN=
GLEAN_SERVER_URL=
# Set to true for contract-only verification without a live /api/chat handler
GLEAN_USE_FIXTURE=false
# Live checklist (ignored when GLEAN_USE_FIXTURE=true):
# Path to a JSON array of { id, title, group, initiallyDone?, dueDate?, askPrompt }
# GLEAN_ONBOARDING_STEPS_FILE=./steps.example.json
# Or inline JSON (same schema)
# GLEAN_ONBOARDING_STEPS_JSON=
