{
  "id": "search-with-discovered-filters",
  "title": "Search Glean with discovered filters",
  "description": "Use the official TypeScript API client to discover the datasources and common filter fields available in your Glean instance, then apply a returned selection to permission-aware Platform Search.",
  "sidebarLabel": "Search with filters",
  "surfaces": ["platform-api"],
  "capabilities": ["search"],
  "status": "quickstart",
  "visibility": "preview",
  "category": "search",
  "level": "Beginner",
  "levels": {
    "minimal": true,
    "wow": false
  },
  "timeEstimate": "~15 min",
  "requiredScopes": ["SEARCH"],
  "authMethod": ["client-api-oauth-or-token"],
  "buildMethod": "scaffold",
  "languages": ["typescript"],
  "prerequisites": [
    "Node 20 or newer",
    "A Glean instance with content indexed",
    "A work email for tenant discovery and OAuth sign-in; a SEARCH-scoped Glean token is the fallback",
    "Experimental Platform APIs enabled through X-Glean-Include-Experimental: true, which the scaffold sends automatically"
  ],
  "demoQueries": [
    {
      "query": "Search for a topic you know exists in your Glean instance",
      "expectedBehavior": "Lists the datasources and common filter fields visible to you, selects a datasource returned by discovery, applies a suggested EQUALS field value when one is available, and returns a valid permission-aware Search response without assuming a fixed result count."
    }
  ],
  "codeAssets": [
    {
      "repoPath": "recipes/search-with-discovered-filters",
      "language": "typescript",
      "description": "Interactive TypeScript CLI using @gleanwork/api-client for Search Filters discovery and filtered Platform Search"
    }
  ],
  "steps": [
    {
      "title": "Scaffold the project",
      "description": "Copies the runnable TypeScript CLI, fixture tests, and sign-in helper into a new directory.",
      "command": "npx -y tiged@2.12.8 --mode=git gleanwork/glean-cookbook/recipes/search-with-discovered-filters search-with-discovered-filters",
      "kind": "scaffold"
    },
    {
      "title": "Install dependencies",
      "command": "cd search-with-discovered-filters && npm install",
      "kind": "install"
    },
    {
      "title": "Check the request flow with fixtures",
      "description": "Exercises the real @gleanwork/api-client with fixture responses to validate catalog discovery, query-backed suggestions, typed field-filter propagation, datasource fallback, and experimental headers without credentials or network access.",
      "command": "cd search-with-discovered-filters && npm test",
      "kind": "manual"
    },
    {
      "title": "Sign in to Glean",
      "description": "Uses your work email to find your Glean tenant and opens the OAuth flow. If OAuth is unavailable, copy .env.example to .env and provide a SEARCH-scoped Glean token instead.",
      "command": "cd search-with-discovered-filters && npm run login -- --email \"<work-email>\"",
      "kind": "authenticate"
    },
    {
      "title": "Verify against your instance",
      "description": "Uses your topic to discover current filters, automatically selects a returned datasource and suggested field value when available, then validates the Search response shape and pagination state.",
      "command": "cd search-with-discovered-filters && npm run verify -- --query \"<search-query>\"",
      "kind": "verify-live"
    },
    {
      "title": "Choose filters and search",
      "description": "The CLI shows the best-effort filter catalog, asks you to choose a datasource and suggested field value, and prints results, warnings, pagination state, and request IDs.",
      "command": "cd search-with-discovered-filters && npm start -- --query \"<search-query>\"",
      "kind": "run"
    }
  ],
  "execution": {
    "type": "cli",
    "questions": [
      {
        "id": "email",
        "prompt": "What is your work email? It is used once to discover your Glean tenant."
      },
      {
        "id": "search-query",
        "prompt": "What topic do you know exists in your Glean content?"
      }
    ],
    "auth": [
      {
        "kind": "oauth-with-token-fallback",
        "scopes": ["SEARCH"],
        "setupCommand": "cd search-with-discovered-filters && npm run login -- --email \"<work-email>\"",
        "configFile": ".env",
        "backendVariable": "GLEAN_SERVER_URL",
        "credentialVariable": "GLEAN_API_TOKEN"
      }
    ],
    "verification": {
      "kind": "automated",
      "command": "cd search-with-discovered-filters && npm run verify -- --query \"<search-query>\"",
      "expectedDuration": "about 1 minute after sign-in",
      "startsOwnServer": false
    },
    "run": {
      "command": "cd search-with-discovered-filters && npm start -- --query \"<search-query>\"",
      "userBrowser": false
    }
  },
  "architecture": [
    {
      "label": "Your CLI",
      "caption": "query and filter choice",
      "icon": "terminal"
    },
    {
      "label": "TypeScript API client",
      "caption": "typed requests and responses",
      "icon": "Code"
    },
    {
      "label": "Search Filters",
      "caption": "visible datasources and fields",
      "category": "search",
      "emphasized": true
    },
    {
      "label": "Platform Search",
      "caption": "permission-aware results",
      "category": "search"
    }
  ],
  "aiPrompt": "Scaffold and run the verified Search with discovered filters quickstart. Ask for the user's work email and one topic they know exists in their Glean instance. Run the fixture tests, sign in with SEARCH scope, run live verification, then start the interactive CLI. Use @gleanwork/api-client's glean.search.listFilters() for catalog and query-backed discovery, then pass only a returned datasource, field, and suggested value to glean.search.query(). Enable experimental features through the SDK's documented X_GLEAN_INCLUDE_EXPERIMENTAL option. Do not recreate the SDK transport, impersonate another user, or claim the best-effort filter catalog is exhaustive.",
  "llmContext": "@gleanwork/api-client provides typed glean.search.listFilters() and glean.search.query() methods for these Platform APIs. Enable the experimental header through X_GLEAN_INCLUDE_EXPERIMENTAL rather than constructing requests manually. Catalog discovery returns datasource identifiers and common built-in fields; query-backed discovery requires exactly one datasource and returns bounded, non-exhaustive suggested values. Apply returned values unchanged with EQUALS. The caller's credential is the permission boundary. Let the SDK validate response data and surface typed API errors; display warnings, request IDs, Retry-After guidance, and next_cursor without logging credentials or storing results.",
  "goDependency": false,
  "featured": true,
  "hidden": false,
  "tags": ["search", "filters", "quickstart", "platform-api"],
  "content": {
    "problem": "Search filters differ across Glean instances and connected sources. Hard-coding a datasource or field from another environment makes a first integration brittle, while hand-writing HTTP hides the supported client developers should use. This quickstart uses the official TypeScript API client to discover what the signed-in user can filter, then carries that returned selection into a typed Search request that matches the reader's own instance and permissions.",
    "takeItFurther": [
      "Follow next_cursor to paginate without treating it as data or constructing it yourself.",
      "Persist the catalog briefly for UI rendering, but refresh it and continue to treat it as best effort rather than an exhaustive schema.",
      "Build a faceted search interface that requests query-specific values after the user chooses one datasource.",
      "Add time_range alongside discovered filters for bounded recency searches."
    ],
    "limitations": [
      {
        "title": "Discovery is best effort",
        "description": "A field omitted from Search Filters may still be valid, and suggested values are bounded and non-exhaustive. Reusing a returned value does not guarantee matching results."
      },
      {
        "title": "Experimental API",
        "description": "Both endpoints may change. The scaffold opts in explicitly and should be evaluated before production adoption."
      }
    ]
  }
}
