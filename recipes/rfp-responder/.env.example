# Your Glean backend URL, e.g. https://acme-be.glean.com — take it from the
# dev site rather than deriving it from an instance name, which only works for
# the default backend naming.
GLEAN_SERVER_URL=

# Your own OAuth access token or Glean API token with the CHAT scope.
# This recipe runs as *you*. There is no impersonation and no act-as: whatever
# you cannot see, the drafts cannot cite. That is the point — see README.
GLEAN_API_TOKEN=

# Comma-separated Glean URL prefixes cleared for customer-facing answers.
RFP_APPROVED_SOURCE_PREFIXES=

# Set true to run entirely from recorded fixtures (no network, no credentials).
GLEAN_USE_FIXTURE=false

PORT=3000
